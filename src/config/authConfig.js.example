import { LogLevel } from '@azure/msal-browser';

// TEMPLATE FILE - Copy to authConfig.js and update with your actual values
// DO NOT commit the actual authConfig.js file with real credentials

export const msalConfig = {
    auth: {
        clientId: 'your-client-id-here', // Replace with your actual Azure AD client ID
        authority: 'https://your-tenant.ciamlogin.com/', // Replace with your actual authority
        redirectUri: 'http://localhost:3001/redirect', // Update for production
        postLogoutRedirectUri: '/', 
        navigateToLoginRequestUrl: false,
    },
    cache: {
        cacheLocation: 'sessionStorage',
        storeAuthStateInCookie: false,
    },
    system: {
        loggerOptions: {
            loggerCallback: (level, message, containsPii) => {
                if (containsPii) {
                    return;
                }
                switch (level) {
                    case LogLevel.Error:
                        console.error(message);
                        return;
                    case LogLevel.Info:
                        console.info(message);
                        return;
                    case LogLevel.Verbose:
                        console.debug(message);
                        return;
                    case LogLevel.Warning:
                        console.warn(message);
                        return;
                    default:
                        return;
                }
            },
        },
    },
};

export const loginRequest = {
    scopes: [
        "openid", 
        "profile",
        "your-scope-here" // Replace with your actual API scope
    ],
};
